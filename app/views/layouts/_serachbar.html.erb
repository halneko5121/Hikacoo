<div class="mt-5 mb-5 text-center">
  <h2>商品価格を比較しましょう！</h2>
</div>
<%= form_with(url: search_path, scope: :search_word, local: true) do |f| %>
  <div class="form-group row">
    <% if @trend_word == nil %>
      <%= f.text_field :title, class: "form-control" %>
    <% else %>
      <%= f.text_field :title, value: @trend_word, class: "form-control" %>
    <% end %>
    <%= f.submit '検索', class: "btn btn-primary" %>
  </div>
<% end %>
<div class="mt-1 pb-1 border-bottom">本日のトレンドワード（ランダムで6個表示してます）</div>
<div class="mt-2 mb-2 col-12 d-flex flex-row">
  <% @trend_word_first.each do |trend_word| %>
    <%= form_with(url: root_url, local: true) do |f| %>
      <%= f.submit trend_word, class: "mr-4 btn btn-success" %>
    <% end %>
  <% end %>
</div>
<div class="mt-2 mb-2 col-12 d-flex flex-row">
  <% @trend_word_second.each do |trend_word| %>
    <%= form_with(url: root_url, local: true) do |f| %>
      <%= f.submit trend_word, class: "mr-4 btn btn-success" %>
    <% end %>
  <% end %>
</div>